meta {
  name: createCompany
  type: http
  seq: 2
}

post {
  url: {{host}}/api/info
  body: json
  auth: inherit
}

body:json {
  {
    "name":"auausalakau",  
    "sector":"maritime",
    "applicableMetrics": ["kgCO2e", "kWh", "m3", "kg", "safety score", "% agricultural product"]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
