# Optional: override API base URL if your backend runs on a different host/port.
# Default is http://localhost:8080 (no trailing slash).
# Example: if backend is on port 8808:
# VITE_API_BASE_URL=http://localhost:8808
